import type { ClubSeed } from "../types";

// Placeholder seed for GFF demo workspace. Real datasets will be added in a
// later phase; for now the demo runs with an empty live=false dataset.
export const gffSeed: ClubSeed = {
  live: false,
  data: {},
};
