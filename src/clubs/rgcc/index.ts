import type { ClubConfig } from "../types";
import { rgccBrand } from "./brand";
import { rgccModules } from "./modules";
import { rgccSeed } from "./seed";

export const rgccClub: ClubConfig = {
  id: "rgcc",
  brand: rgccBrand,
  modules: rgccModules,
  seed: rgccSeed,
};
